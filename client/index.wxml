<!--index.wxml-->
<button class='btn' bindtap='jump'> 新增新闻 </button>
<view wx:for="{{array}}">
  <view class='item' id='{{item.id}}' bindtap='short'>
    {{item.id}}-{{item.title}}
  </view>
</view>

